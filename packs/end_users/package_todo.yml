# This file contains a list of dependencies that are not part of the long term plan for the
# 'packs/end_users' package.
# We should generally work to reduce this list over time.
#
# You can regenerate this file using the following command:
#
# bin/packwerk update-todo
---
".":
  "::ApplicationController":
    violations:
    - privacy
    files:
    - packs/end_users/app/controllers/mypages_controller.rb
  "::ApplicationRecord":
    violations:
    - privacy
    files:
    - packs/end_users/app/models/end_user.rb
  "::Serializer::Base":
    violations:
    - privacy
    files:
    - packs/end_users/app/controllers/serializer/mypages_show.rb
